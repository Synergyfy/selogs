{
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@10.29.1 --activate && pnpm i --filter vehicle-capture-system...",
  "buildCommand": "cd ../.. && pnpm --filter vehicle-capture-system build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
